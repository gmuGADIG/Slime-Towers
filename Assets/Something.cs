using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Something : MonoBehaviour
{
    // Start is called before the first frame update
    //This does nothing wow!!
    void Start()
    {
        Debug.Log("WOWOWOWOW!!!!!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
